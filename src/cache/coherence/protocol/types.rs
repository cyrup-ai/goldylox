//! Core types and data structures for coherence protocol
//!
//! This module defines additional types used throughout the coherence protocol implementation.

// Removed re-export to eliminate type identity conflicts
// Use direct import: crate::cache::coherence::data_structures::CoherenceController
